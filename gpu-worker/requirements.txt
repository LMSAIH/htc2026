# GPU Worker dependencies
# For simulated mode, only httpx is strictly needed.
# The ML libs are included so the image is ready for real training later.

httpx>=0.27,<1.0
torch>=2.3,<3.0
transformers>=4.41,<5.0
datasets>=2.19,<3.0
huggingface-hub>=0.23,<1.0
peft>=0.11,<1.0
accelerate>=0.30,<1.0
boto3>=1.34,<2.0
Pillow>=10.0,<11.0
